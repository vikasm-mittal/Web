var app;
(function (app) {
    var WeatherResult = (function () {
        function WeatherResult(results) {
            this.results = results;
            this.results = results;
        }
        return WeatherResult;
    }());
    app.WeatherResult = WeatherResult;
})(app || (app = {}));
